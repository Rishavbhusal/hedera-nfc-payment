# Relayer Configuration
RELAYER_PRIVATE_KEY=your_relayer_private_key_here

# Alchemy API
ALCHEMY_API_KEY=your_alchemy_api_key_here

# Web Push (VAPID Keys) - Generate with: node -e "const webpush = require('web-push'); console.log(webpush.generateVAPIDKeys());"
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:mail@taraxio.com

# Database (PostgreSQL)
# Railway: railway link && railway add postgres && railway variables
# Local: postgresql://localhost:5432/tapthatx_dev
DATABASE_URL=postgresql://user:password@host:port/database
